{% trans_default_domain 'FOSUserBundle' %}

<h1>MODIFICATION LE PROFILE</h1>
    <hr>
<div >
        <p class="text-right">
            <a class ="btn btn-success" href="{{ path('jp_fiche_paie_compte')  }}"> <i class="fa fa-reply"></i> Retour à la liste</a>
        </p>
    </div>

{{ form_start(form, { 'action': path('fos_user_profile_edit'), 'attr': { 'class': 'well' } }) }}
    {{ form_widget(form) }}
    <div>
        <input class="btn btn-success" type="submit" value="{{ 'profile.edit.submit'|trans }}" />
    </div>
{{ form_end(form) }}

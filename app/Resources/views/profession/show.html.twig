{% extends 'base.html.twig' %}

{% block body %}
    <h1>Profession</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ profession.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ profession.designation }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('profil_member_profession_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('profil_member_profession_edit', { 'id': profession.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}

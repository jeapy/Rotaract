{% extends 'base.html.twig' %}

{% block body %}
    <h1>Organe</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ organe.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ organe.designation }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('profil_member_organe_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('profil_member_organe_edit', { 'id': organe.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
